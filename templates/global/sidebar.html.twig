<div class="card">
    <div class="card-body">
        <a href="{{ path('question_create') }}" class="btn btn-primary">Ask question</a>
    </div>
</div>

<div class="card">
    <div class="card-body">
        {% for tag in tags %}
            <a href="#" class="btn btn-sm btn-light">{{ tag.name }}</a>
        {% endfor %}
    </div>
</div>